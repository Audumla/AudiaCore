name: audiacore-revalidation

on:
  push:
    branches:
      - 'revalidation/**'
  pull_request:
    paths:
      - 'Cargo.toml'
      - 'Cargo.lock'
      - 'rust-toolchain.toml'
      - 'crates/**'
      - 'examples/**'
      - 'scripts/revalidate.sh'
      - 'docs/architecture/**'
      - 'AGENTS.md'
      - '.github/workflows/revalidation.yml'

concurrency:
  group: audiacore-revalidation-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  architecture:
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-24.04
          - macos-15
          - windows-2025
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: '1.95.0'
          components: rustfmt, clippy
      - name: Resolve lockfile for Stage 3B dependency decision
        shell: bash
        run: cargo generate-lockfile
      - name: Revalidate current stage
        shell: bash
        run: bash scripts/revalidate.sh
      - name: Capture Stage 3B resolved lockfile
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: audiacore-stage3b-lockfile-${{ matrix.os }}
          if-no-files-found: error
          path: Cargo.lock
