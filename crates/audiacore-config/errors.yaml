VAL-CONFIG-001:
  message: "Configuration layer id must not be empty."
  resolution: "Provide a non-empty identity for every configuration layer."
CFG-CONFIG-001:
  message: "Configuration layer is invalid TOML."
  resolution: "Correct the TOML content for the identified configuration layer."
CFG-CONFIG-002:
  message: "Configuration resolution failed."
  resolution: "Correct the effective configuration so it matches the requested typed schema."
